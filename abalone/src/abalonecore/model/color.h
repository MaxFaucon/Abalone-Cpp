#ifndef COLOR_H
#define COLOR_H

/**
 * @brief The Color enum Enumeration that represents the differents colors that
 * an hexagon can have.
 * NONE is for the hexagons without marbles.
 */
enum Color
{

    WHITE = 1, BLACK = 2, NONE = 3

};

#endif // COLOR_H
